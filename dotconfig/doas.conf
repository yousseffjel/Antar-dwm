# doas configuration
# Replace 'USERNAME' with your actual username
# 'persist' means password is remembered for 5 minutes after first use
# 'nopass' means no password required (less secure, use with caution)
permit persist keepenv USERNAME as root
# Alternative (less secure): permit nopass USERNAME as root